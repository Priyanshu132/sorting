#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
int prime(long int);
int main()
{
  long int n;
  int *a;
    long int i,j,k=0,p=0;
       scanf("%ld",&n);
    a=malloc((n/2+1)*sizeof(int*));
    for(i=2;i<n;i++){
        p=0;
        if(i==2||i==3){
            a[k]=i;
        k++;}
        else{
            for(j=2;j<i-1;j++){
                if(i%j==0){
                    p++;
                }
            }
            if(p<1){
            a[k]=i;k++;}
    }}
    for(i=0;i<k;i++)
        printf("%d  ",a[i]);
    return (0);
}
                
    
